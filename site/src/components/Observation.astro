---
interface Props {
	items?: string[];
}

const { items } = Astro.props;
---

<!-- Pass `items`, or write <li> elements into the slot. -->
<ul class="observation">
	{items ? items.map((t) => <li set:html={t} />) : <slot />}
</ul>
